# hotel_management_system_using_GUI_tkinter
**🏨 Hotel Management System (Tkinter + Pickle)**

A desktop Hotel Management System built with Python (Tkinter GUI) and simple file-based storage using pickle.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**🚀 Key Features**

**🧾 Check-In** — Add guest details (name, address, mobile, days), choose room type and payment method, auto-allocation of available room number and bill calculation.

**📋 Show Guest List** — View all current guests and assigned room numbers.

**🧾 Receipt Generation**— Creates a receipt (text popup) after successful check-in.

**🔎 Get Info** — Look up guest details by room number (name, address, mobile, bill).

**✅ Check-Out** — Remove guest entry (room becomes available again).

**💾 Persistent Storage** — Guest records stored in hotel.dat using pickle.

**🖥️ Tkinter GUI** — Button-driven desktop UI; some screens auto-generated by PAGE.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**⚙️ Requirements**

Python 3.7+ (recommended)

Standard library modules: tkinter, pickle, os, sys, subprocess

On some Linux distributions, tkinter must be installed separately (see below).

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**🛠 Installation & Setup**

Clone the repo:

git clone https://github.com/<your-username>/hotel-management.git
cd hotel-management

(Optional) Create and activate a virtual environment:

python -m venv venv
 **Windows**
venv\Scripts\activate
 **Linux / macOS**
source venv/bin/activate

**Ensure tkinter is available:**

**Ubuntu / Debian**

sudo apt update
sudo apt install python3-tk

**Fedora**

sudo dnf install python3-tkinter

**Windows / macOS:** tkinter usually ships with the official Python installer. If missing, reinstall Python from python.org.

**▶ How to run**

Run the main launcher:

python mainly.py

No external PyPI packages required.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**💾 Data storage**

Records are serialized into hotel.dat using Python's pickle module.

Each record is an instance of a save class containing:

name, address, mobile_no, room_no, price

recipt.txt (temporary) is generated for receipt display.

Important: pickle is not secure for untrusted input — do not load hotel.dat from untrusted sources.

**🧰 Common fixes / tips**

If your editor underlines tkinter imports but the program runs fine, the IDE may be using a different interpreter. Ensure VS Code / PyCharm uses the same Python interpreter where tkinter is installed.

On Linux, install python3-tk (see above).

If the GUI fails to open, run from a terminal to see error messages:

python mainly.py
